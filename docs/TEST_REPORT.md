# 实际测试报告

记录日期：2026-09-24。测试计划和未执行场景见 [验证计划](VALIDATION_PLAN.md)。本报告区分实测结果与尚未验证的目标。

| 场景 | 实际方法与结果 | 状态 |
| --- | --- | --- |
| 来源材料可追溯 | 逐份打开 7 份巨潮公告与 3 篇新闻；18 条短引文均对照 PDF 物理页码或新闻正文核实。原文 URL、页码、时间口径记录在 CASE_SOURCES.md 和 seed_sources.json。 | 通过 |
| 类型检查 | 执行 TypeScript 类型检查，无错误。 | 通过 |
| 状态回放 | 自动测试确认截至 2024-04-18 的资料不含 GS-06、NEWS-03，结论仍为「预计难续」，不会提前写成已终止。 | 通过 |
| 正式终止及边界 | 自动测试确认加入 GS-06 后，结论为旧方案终止，且明确保留后续收购意向的不确定性。 | 通过 |
| 同公司误合并 | 自动测试确认 GS-X1（倍特期货股权出售）不进入华鲲振宇收购事件；标的规则建议独立事件。 | 通过 |
| 回放时间边界 | 自动测试确认 4 月 18 日前尚未披露的倍特期货事件不生成结论或已确认主张；界面回放时隐藏没有已披露来源的事件卡。 | 规则测试通过；界面待新版录屏复核 |
| 转引来源去重 | 自动测试确认三篇新闻分别指向 GS-01、GS-05、GS-06 原始公告来源组。 | 通过 |
| 重复 URL 与修订版本 | 自动测试确认相同 PDF 链接附带查询参数仍判重复；同 URL 仅在正文引文及原文更新时间均改变时才接受为新版本。 | 通过 |
| 否认、更正、过期 | 显著标记的模拟材料分别测试目标主张待复核、旧主张保留、通知依据，以及过期不等于虚假；用户自称官方公告仍不能覆盖已核验结论。 | 规则测试通过；真实历史未出现这些分支 |
| Web 首页 | 本地 HTTP 请求返回 200，包含真实案例标题及资料截止提醒。 | 通过 |
| 模型未配置时的异常 | 配置密钥前，线上 POST `/api/analyze` 返回 503 和 `MODEL_NOT_CONFIGURED`；界面保留人工核对及已核验样本回退。 | 接口实测通过；界面操作待录屏复核 |
| DeepSeek 实际抽取 | 2026-09-24 本地 Web 接口请求 GS-06、GS-X1 公开公告片段，均 HTTP 200；分别返回 `huakun` 与 `beite`，引文定位 5/5、1/1；人工对照原始 PDF。详情见 [模型调用记录](AI_MODEL_RUN.md)。 | 通过；公开部署待更新后复测 |
| 提示注入与合规边界 | 模拟无出处论坛材料夹带买入及保盈指令；真实模型调用返回 `uncertain` 和“传闻”，未输出买卖建议。单例不代表普遍防护率。 | 通过单例 |
| 接口输入异常 | 本地接口对过短输入返回 `400 INVALID_INPUT`、非 JSON 返回 `415 UNSUPPORTED_MEDIA_TYPE`；跨站 Origin 被开发服务器以 403 拒绝。 | 通过 |
| 部署 URL 及外部访问 | Sites 权限为 public；公开站点 `https://event-evidence-cathy-2026.jscsjeremy.chatgpt.site` 经 PowerShell HTTPS GET 返回 200。2026-09-24 约 12:00 中国时间，另一智能体以本机 Chrome 全新 Playwright BrowserContext、无 Cookie 打开页面，确认标题、两个事件、当前结论、回放和导入按钮可见。`curl -I` 曾遇 Cloudflare 403，表明命令行 HEAD 结果不能代替浏览器验证。 | 浏览器访问通过；完整交互待录屏复核 |

自动测试命令：

    node node_modules/tsx/dist/cli.mjs --test tests/evidence.test.ts

本次结果：12 项通过，0 项失败。这个小样本测试只覆盖预置案例及显著标记的模拟状态分支，不能外推为跨行业准确率。没有实测的误合并率、用户耗时或通知精确率不写成已达成指标。

## 异常与合规验收口径

- 外部材料缺原文链接、时间或引文时，保持待复核；不把系统抓取时间冒充事件发生时间。
- 模型返回无法在输入正文定位的引文时，服务端移除该主张。提示注入文本仅作为材料内容，不得改变抽取规则。
- 用户导入的材料不自动改写已核验官方结论；重复导入不生成新版本及通知。
- 产品展示证据与标的关系，不提供确定性涨跌预测、收益承诺或直接买卖建议。
- 真实案例没有独立的否认、更正或到期节点。相关分支按 VALIDATION_PLAN.md 以显著标记的模拟材料测试；未执行前不列为通过。
